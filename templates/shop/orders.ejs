<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../includes/head.ejs') %>
</head>
    
<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <h1># <%= order.id %></h1>
        <ul>
            <% order.products.forEach(prod => { %>
            <li><%= prod.title %> (<%= prod.orderItem.quantity %>)</li>
            <% }); %>
        </ul>
    </main>
</body>

</html>